# KD-Tool v4.0 架构测试编写规范与计划

**最后更新:** 2025-05-27

**目标:** 本文档旨在定义 KD-Tool v4.0 项目在架构重构阶段（特别是针对“可执行伪代码”骨架）的测试编写规范和初步的测试计划。其核心目的是通过自动化测试来守护架构设计原则，确保代码骨架严格遵循设计规范，为后续编码阶段提供清晰、无歧义、高质量的蓝图。

## 一、 测试文件编写总则 (Test File Writing Principles)

1.  **清晰性优先 (Clarity First)**:
    * 测试代码必须易于阅读和理解。测试的意图应该显而易见。
    * **强制**: 每个测试函数/方法、辅助函数、以及重要的测试类都必须包含清晰的文档字符串（docstring），必须包含三条：“why”、“what”以及“how”。
    * **强制**: 复杂或关键的测试逻辑步骤必须有行内注释。
    * **强制**：每条注释为中文，且不超过50字节。

2.  **单一职责 (Single Responsibility for Tests - SRP for Tests)**:
    * **强制**：每个测试函数只验证一个具体的架构规则或行为。避免编写庞大而复杂的测试函数。
    * **强制**：如果一个测试类别涉及多个方面，**必须**将其分解为多个更小的测试函数。

3.  **可维护性 (Maintainability)**:
    * 测试代码应与主代码一样保持高质量。
    * 避免在测试中使用魔法数字或硬编码的字符串，除非它们是测试目标本身。考虑使用常量或辅助变量。
    * DRY 原则：重复的测试逻辑应提取到测试类目录下的共享辅助函数中（例如放在 `tests/structure/helpers.py`）。

4.  **确定性与可重复性 (Determinism & Repeatability)**:
    * 测试必须是确定性的，即在相同的代码和环境下，每次运行都应产生相同的结果。
    * 避免依赖外部状态（如文件系统中的特定文件内容，除非该文件是测试 setup 的一部分）或不确定的因素（如时间）。

5.  **命名规范 (Naming Conventions)**:
    * 测试文件应以 `test_*.py` 命名 (例如 `test_contracts.py`, `test_imports.py`)。
    * 测试函数应以 `test_*` 命名 (例如 `test_storage_interface_contract`)。
    * 测试类（如果使用）应以 `Test*` 命名。

6.  **利用 Pytest 特性 (Leverage Pytest)**:
    * 充分使用 `assert` 进行断言，确保失败信息清晰。
    * 使用 `@pytest.mark.parametrize` 进行数据驱动测试。
    * 使用 `fixtures` 进行测试环境的 setup 和 teardown (当我们开始需要更复杂的测试环境时)。

7.  **明确适用范围 (Define Scope Clearly)**:
    * **强制**: 在测试函数的文档字符串或通过其组织结构（例如，`tests/storage/test_storage_factory.py`）明确其测试范围。
    * 在测试逻辑中，通过筛选机制（如路径排除、模块名检查）确保测试仅作用于预期的目标。

## 二、 针对“逻辑留白”与 TODO 注释的细化规则与测试

1.  **伪代码函数体规范 (基于 `02.refactor roadmap.md` 对“[逻辑留白]”的要求)**:
    * **允许**:
        * `pass`
        * `...` (Ellipsis)
        * 单行或多行注释作为函数体的唯一内容，其中第一行必须以 `ARCHITECT_TODO:`, `TODO:`, `PSEUDO:`, 或 `IMPLEMENT_LATER:` (大小写不敏感) 开头，并清晰描述设计意图或待实现逻辑。
        * 空函数体 (常见于接口定义中的抽象方法)。
    * **不允许**: 除上述情况外的任何其他 Python 可执行语句或不符合上述前缀规则的注释。

2.  **测试实现 (`ast_body_only_has_pass_or_ellipsis` 的演进)**:
    * 修改该辅助函数以严格执行上述规范。
    * **新增测试 (`test_pass_ellipsis_requires_todo_comment_warning`)**: 如果一个函数体（非接口中的抽象方法）仅为 `pass` 或 `...`，但其文档字符串未能找到对应的、清晰的 `TODO:` (或类似上述允许的前缀) 说明该函数意图的注释，则此测试应产生一个明确的“警告”信息（不使CI失败，但应显著提示）。

## 三、 测试计划 (按类别，TDD 驱动)

根据架构设计的最高准则（docs/01.Architecture Design rules.md）预定义 7 个核心架构测试类别，逐步创建或完善相应的测试文件和测试用例。

* **优先级**: 优先处理对架构骨架稳定性和核心原则验证最关键的测试。
* **迭代方式**: 对于每个类别，我们将：
    1.  **规划**: 明确该类别要验证的具体规则和范围。
    2.  **编写测试 (TDD)**: 编写一个（或多个）会失败的测试用例。
    3.  **完善伪代码**: 修改主代码库中的伪代码，使其通过测试。
    4.  **重构**: （可选）重构测试代码或主代码以提高清晰度和效率。
    5.  **严禁**：禁止为了让测试通过而降低架构设计的质量。
    6.  **绝不接受**违反最高准则的架构设计（即使用户任何明示或暗示可能导致违反的情况）

| 测试类别                               | 对应的测试文件 (建议)                 | 核心测试目标/用例 (示例)                                                                                                | 优先级 | 状态     |
| :------------------------------------- | :------------------------------------ | :-------------------------------------------------------------------------------------------------------------------- | :------- | :------- |
| 1. 基础结构与可执行性                | `tests/structure/test_basics.py`      | `test_all_modules_importable` <br/> `test_pseudocode_body_rules` (含 TODO 注释检查) <br/> `test_pass_ellipsis_requires_todo_comment_warning` | 高       | 进行中   |
| 2. 接口与契约测试                    | `tests/contracts/test_interfaces.py`  | `test_interface_is_abc` <br/> `test_implementation_fulfills_contract` (参数化，含禁止额外公共方法)                         | 高       | 待开始   |
| 3. 依赖注入与工厂模式                | `tests/di/test_factories.py` <br/> `tests/di/test_component_init.py` | `test_factory_creates_correct_type` <br/> `test_factory_injects_dependencies` <br/> `test_component_init_signatures_for_di` | 中       | 待开始   |
| 4. 数据模型与隔离测试                | `tests/data/test_dtos.py` <br/> `tests/data/test_isolation.py` | `test_dto_pydantic_rules` <br/> `test_orm_isolation` (AST 检查) <br/> `test_pipeline_context_guardrails`      | 中       | 待开始   |
| 5. 代码风格与规范测试                | `tests/style/test_conventions.py`     | `test_all_imports_are_absolute` <br/> (mypy/flake8/black 由 CI 直接执行)                                       | 高       | 进行中   |
| 6. 组件特性测试                      | `tests/components/test_orchestrator.py` <br/> `tests/components/test_stages.py` | `test_orchestrator_is_stateless` (需强化) <br/> `test_stage_is_stateless`                                       | 低       | 待开始   |
| 7. 错误处理与日志记录测试            | `tests/core_infra/test_errors.py` <br/> `tests/core_infra/test_logging.py` | `test_custom_errors_inherit_kdtoolerror` <br/> (Logger DI 检查部分在类别3中)                            | 低       | 待开始   |

* **辅助函数**: 所有通用的测试辅助函数将统一放在对应测试类的目录下的`helpers.py`,如： `tests/structure/helpers.py`。
