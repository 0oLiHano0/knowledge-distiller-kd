**任务ID:** `KD-DOCS-001`

---

**一、 背景信息:**

*   在 Phase 1 和 Phase 2 完成后，项目的架构和实现（特别是配置管理、依赖注入、存储交互和日志记录方面）发生了显著变化。现有的架构设计文档 (`03.Architecture Design Dcoument v3.0.md`) 需要更新以准确反映这些变化。

----

**二、任务目标 (Objective):**

*   更新 `docs/03.Architecture Design Dcoument v3.0.md`，使其内容与当前代码实现保持一致，特别是在 Core 层、Storage 层、配置管理和日志记录方面。
*   最终审查并确认 `docs/roadmap_phase1_integration_logging.md` 的准确性。

---

**三、文件位置:**

*   `docs/03.Architecture Design Dcoument v3.0.md` (修改)
*   `docs/roadmap_phase1_integration_logging.md` (审阅)

---

**四、子任务列表:**

1.  **更新 Core 层描述:**
    *   修改 `KnowledgeDistillerEngine` 的描述，说明其通过 `__init__` 接收 `StorageInterface`, `AppConfig`, `logger` 依赖。
    *   提及 `core/factories.py` 用于创建和提供这些依赖。
    *   移除或修改关于 Engine 内部初始化依赖的旧描述。
    *   更新 API 规范部分（7.1 核心引擎 API），反映 `__init__` 的变化。
2.  **更新 Storage 层描述:**
    *   强调 Core 引擎通过 `StorageInterface` 与存储层交互。
    *   确认接口方法描述 (`StorageInterface` API 规范部分 7.2) 与实际接口定义一致。
3.  **添加配置管理说明:**
    *   可能需要在架构文档中新增一节（或在技术栈/设计原则中扩展）来描述配置管理机制。
    *   说明使用 `core/config.py` 中的 Pydantic 模型 (`AppConfig` 等)。
    *   说明配置加载优先级（环境变量 -> `.env` 文件 -> 默认值）。
4.  **更新日志记录说明:**
    *   明确项目使用 Loguru 进行日志记录。
    *   描述通过 `LoggingConfig` 进行配置（文件路径、级别、轮转、JSON 格式等）。
    *   说明 logger 实例通过工厂提供和注入。
5.  **更新技术栈/依赖:**
    *   确保 `pydantic-settings` 已添加到依赖列表中。
    *   确认其他库版本是否需要更新。
6.  **审阅 Phase 1 路线图:**
    *   通读 `docs/roadmap_phase1_integration_logging.md`，确保其内容准确反映了 Phase 1 实际完成的工作。
7.  **整体审查和格式化:** 通读整个架构文档，确保一致性、清晰度和格式正确。

---

**五、验收标准:**

1.  `docs/03.Architecture Design Dcoument v3.0.md` 已更新，准确反映了配置管理、依赖注入、存储接口交互和 Loguru 日志记录的实现。
2.  文档中关于 Core 层、Storage 层、技术栈、API 规范的相关部分已更新。
3.  `docs/roadmap_phase1_integration_logging.md` 已审阅确认。
4.  文档内容清晰、一致，格式正确。

---

**六、其他:**

*   这是确保文档与代码同步的关键步骤。
*   作为 Phase 1 的收尾工作。 
