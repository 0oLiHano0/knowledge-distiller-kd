```python

# knowledge_distiller_kd/core/orchestrator_factory.py
"""
此模块定义了 OrchestratorFactory，负责创建 Orchestrator 实例。
它遵循类式工厂模式，确保 Orchestrator 的创建过程被封装，
并正确注入其所有依赖。
它遵循将工厂与其核心组件共置的原则。
"""

from typing import Dict, List
from loguru import Logger

# --- 从 schemas 和 core 模块导入核心定义 ---
from knowledge_distiller_kd.schemas.settings_models import OrchestratorSettings
from knowledge_distiller_kd.core.interfaces import StageInterface
from knowledge_distiller_kd.core.orchestrator import Orchestrator
from knowledge_distiller_kd.core.errors import KDToolError  # 导入统一的基础错误

# --- 工厂相关的自定义异常 ---
class FactoryError(KDToolError):  # 继承自 KDToolError
    """与工厂操作相关的基本异常。"""
    pass

class OrchestratorCreationError(FactoryError):
    """当 Orchestrator 创建失败时抛出。"""
    def __init__(self, message: str, original_exception: Exception):
        # 保持详细的错误信息，同时作为 FactoryError 的子类
        full_message = f"Orchestrator 创建失败: {message}"
        super().__init__(message=full_message, original_exception=original_exception)
        # self.original_exception = original_exception # KDToolError 基类已处理

# --- 类式工厂定义 ---
class OrchestratorFactory:
    """
    一个类式工厂，负责创建和配置 Orchestrator 实例。
    """
    def __init__(self, logger: Logger):
        """
        初始化工厂。

        参数:
            logger (Logger): 用于日志记录的 Logger 实例。
        """
        # 为工厂自身绑定上下文
        self._logger = logger.bind(factory="OrchestratorFactory")
        self._logger.info("OrchestratorFactory 实例已创建。")

    def create(self,
               stage_modules: Dict[str, StageInterface],
               default_stage_order: List[str],
               settings: OrchestratorSettings
               ) -> Orchestrator:
        """
        创建并返回一个配置好的 Orchestrator 实例。

        参数:
            stage_modules (Dict[str, StageInterface]): 阶段名称到阶段模块实例的字典。
            default_stage_order (List[str]): 默认的阶段执行顺序列表。
            settings (OrchestratorSettings): Orchestrator 的配置设置实例。

        返回:
            Orchestrator: 一个准备好使用的 Orchestrator 实例。

        可能抛出的异常:
            OrchestratorCreationError: 如果 Orchestrator 初始化失败。
        """
        self._logger.info("尝试创建 Orchestrator 实例...")
        self._logger.debug(f"接收到 {len(stage_modules)} 个阶段模块，默认顺序: {default_stage_order}")
        self._logger.debug(f"使用的 OrchestratorSettings: {settings}")

        try:
            # --- 创建 Orchestrator 实例 ---
            # 注入依赖，并为 Orchestrator 创建一个带有 'component' 上下文的 Logger
            orchestrator_instance = Orchestrator(
                stage_modules=stage_modules,
                default_stage_order=default_stage_order,
                settings=settings,
                logger=self._logger.bind(component="Orchestrator") # 注入更具体的 Logger 上下文
            )
            self._logger.success("Orchestrator 实例已成功创建。") # 使用 success 级别表示成功
            return orchestrator_instance

        except Exception as e:
            # 记录异常并抛出我们自定义的、更具体的错误
            self._logger.exception("创建 Orchestrator 实例时发生未预料的错误。") # 使用 exception 自动记录堆栈信息
            raise OrchestratorCreationError(message=str(e), original_exception=e) from e

```